@model IEnumerable<University.Models.Poliza>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Asegurado.NombreCompleto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroPoliza)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Asegurado.NombreCompleto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroPoliza)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PolizaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PolizaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PolizaID })
        </td>
    </tr>
}

</table>
